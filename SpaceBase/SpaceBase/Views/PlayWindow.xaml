<Window x:Class="SpaceBase.PlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:input="clr-namespace:System.Windows.Input;assembly=netstandard"
        xmlns:local="clr-namespace:SpaceBase"
        d:DataContext="{d:DesignInstance Type=local:PlayWindowViewModel}"
        mc:Ignorable="d"
        Title="Play Window" Height="450" Width="800" MinHeight="450" MinWidth="800"
        x:Name="PlayWindow2">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Height" Value="25"/>
                <Setter Property="Width" Value="50"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Space Base" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Grid.Row="1" Content="Play" Command="{Binding PlayGameCommand}" CommandParameter="{Binding ElementName=PlayWindow2}"/>
        <Button Grid.Row="2" Content="Quit" Command="{Binding QuitGameCommand}"/>
    </Grid>
</Window>
