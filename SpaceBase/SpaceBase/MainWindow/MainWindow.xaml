<Window x:Class="SpaceBase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceBase"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        x:Name="MainGameWindow"
        Title="MainWindow"
        Height="1300" MinHeight="1300"
        Width="1024" MinWidth="1024" MaxWidth="1024"
        Background="Black"
        Closed="MainGameWindow_Closed">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="DarkGray">
            <Grid.Resources>
                <Style TargetType="Label">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="20"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="{Binding Game.CurrentPlayerID, Converter={StaticResource IDToPlayerTextConverter}, Mode=OneWay}"/>

            <Grid Grid.Column="1" Background="Navy">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="{Binding Dice1, Converter={StaticResource DiceToTextConverter}, ConverterParameter=1, Mode=OneWay}"/>
                <Label Grid.Column="1" Content="{Binding Dice2, Converter={StaticResource DiceToTextConverter}, ConverterParameter=2, Mode=OneWay}"/>
            </Grid>
        </Grid>

        <Canvas Grid.Row="1" AllowDrop="True">
            <Canvas.Resources>
                <Style x:Key="DraggableCardStyle" TargetType="local:CardControl">
                    <Setter Property="AllowDrop" Value="True"/>
                    <Setter Property="Panel.ZIndex" Value="1"/>
                    <EventSetter Event="MouseMove" Handler="CardControl_MouseMove"/>
                </Style>
                <Style TargetType="Rectangle">
                    <Setter Property="Stroke" Value="Black"/>
                    <Setter Property="Height" Value="{x:Static local:Constants.CardHeight}"/>
                    <Setter Property="Width" Value="{x:Static local:Constants.CardWidth}"/>
                    <Setter Property="Panel.ZIndex" Value="1"/>
                    <Setter Property="Fill" Value="Blue"/>
                </Style>
                <Style x:Key="CardPoolControlStyle" TargetType="ItemsControl">
                    <Setter Property="Width" Value="600"/>
                    <Setter Property="Height" Value="{x:Static local:Constants.CardHeight}"/>
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <WrapPanel Background="Transparent"/>
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ItemTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <local:CardControl Style="{StaticResource DraggableCardStyle}" Margin="30,0,0,0"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Canvas.Resources>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop1}"/>
            <ItemsControl Style="{StaticResource CardPoolControlStyle}" Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop1}" ItemsSource="{Binding Game.Level1Cards}"/>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop2}"/>
            <ItemsControl Style="{StaticResource CardPoolControlStyle}" Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop2}" ItemsSource="{Binding Game.Level2Cards}"/>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop3}"/>
            <ItemsControl Style="{StaticResource CardPoolControlStyle}" Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop3}" ItemsSource="{Binding Game.Level3Cards}"/>

            <Grid Canvas.Left="0" Canvas.Bottom="0" Width="{Binding Width, ElementName=MainGameWindow}" Height="344" Background="Transparent" IsHitTestVisible="True" DataContext="{Binding HumanPlayer}">
                <ItemsControl x:Name="SectorItemsControl" Grid.Row="0" ItemsSource="{Binding Board.Sectors}" Margin="10,0,10,20" Loaded="SectorItemsControl_Loaded" VerticalAlignment="Bottom">
                    <ItemsControl.Template>
                        <ControlTemplate>
                            <Border BorderBrush="Black" BorderThickness="1">
                                <ItemsPresenter />
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Background="LightGreen"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:SectorView />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Canvas>

        <Grid Grid.Row="2" Panel.ZIndex="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="LightGray" Margin="10,20,10,20">
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource BubbleBorderStyle}" Background="Yellow"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding HumanPlayer.Credits, Mode=OneWay}"/>

                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource BubbleBorderStyle}" Background="LightGreen"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HumanPlayer.Income, Mode=OneWay}"/>

                <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource BubbleBorderStyle}" Background="DodgerBlue"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding HumanPlayer.VictoryPoints, Mode=OneWay}"/>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Height" Value="50"/>
                        <Setter Property="Width" Value="100"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                    </Style>
                </Grid.Resources>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Roll dice" Command="{Binding RollDiceCommand}"/>
                <Button Grid.Column="1" Content="Don't buy" Command="{Binding DontBuyCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
