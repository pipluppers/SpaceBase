<Window x:Class="SpaceBase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceBase"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        x:Name="MainGameWindow"
        Title="MainWindow" Height="1000" MinHeight="1000" Width="1024" MinWidth="1024" MaxWidth="1024" Closed="MainGameWindow_Closed">
    <Window.Resources>
        <local:NullVisibilityConverter x:Key="NullVisibilityConverter"/>
        <local:BoolVisibilityConverter x:Key="BoolVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Canvas x:Name="MainCanvas" Grid.Row="0" Grid.RowSpan="2" AllowDrop="True" Height="1000">
            <Canvas.Resources>
                <Style x:Key="DraggableCardStyle" TargetType="local:CardControl">
                    <Setter Property="AllowDrop" Value="True"/>
                    <Setter Property="Panel.ZIndex" Value="1"/>
                    <EventSetter Event="MouseLeftButtonDown" Handler="CardControl_MouseLeftButtonDown"/>
                    <EventSetter Event="MouseMove" Handler="CardControl_MouseMove"/>
                </Style>
                <Style TargetType="Rectangle">
                    <Setter Property="Stroke" Value="Black"/>
                    <Setter Property="Height" Value="{x:Static local:Constants.CardHeight}"/>
                    <Setter Property="Width" Value="{x:Static local:Constants.CardWidth}"/>
                    <Setter Property="Panel.ZIndex" Value="1"/>
                    <Setter Property="Fill" Value="Blue"/>
                </Style>
            </Canvas.Resources>

            <Button Canvas.Top="10" Canvas.Right="10" Visibility="{Binding IsMainWindowActive, Converter={StaticResource BoolVisibilityConverter}, ConverterParameter=Invert}" Content="Herro" Command="{Binding ReturnToDiceControlCommand}"/>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop1}"/>

            <ItemsControl Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop1}" ItemsSource="{Binding Game.Level1Cards}" Height="{x:Static local:Constants.CardHeight}" Width="600">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:CardControl Style="{StaticResource DraggableCardStyle}" Margin="30,0,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop2}"/>

            <ItemsControl Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop2}" ItemsSource="{Binding Game.Level2Cards}" Height="{x:Static local:Constants.CardHeight}" Width="600">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:CardControl Style="{StaticResource DraggableCardStyle}" Margin="30,0,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Rectangle Canvas.Left="{x:Static local:Constants.CardPoolDeckLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop3}"/>
            
            <ItemsControl Canvas.Left="{x:Static local:Constants.CardPoolLeft}" Canvas.Top="{x:Static local:Constants.CardPoolTop3}" ItemsSource="{Binding Game.Level3Cards}" Height="{x:Static local:Constants.CardHeight}" Width="600">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:CardControl Style="{StaticResource DraggableCardStyle}" Margin="30,0,0,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Canvas.Left="0" Canvas.Top="600" Width="{Binding Width, ElementName=MainGameWindow}" Height="244" Background="Transparent" IsHitTestVisible="True" DataContext="{Binding HumanPlayer}">
                <ItemsControl x:Name="SectorItemsControl" Grid.Row="0" ItemsSource="{Binding Board.Sectors}" Margin="10,0,10,20" Loaded="SectorItemsControl_Loaded">
                    <ItemsControl.Template>
                        <ControlTemplate>
                            <Border BorderBrush="Black" BorderThickness="1">
                                <ItemsPresenter />
                            </Border>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Background="LightGreen"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Black" BorderThickness="1" Width="82" Padding="10,10,10,10" AllowDrop="True" Drop="Border_Drop" IsHitTestVisible="True">
                                <Grid AllowDrop="False">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <local:CardControl Grid.Row="0" DataContext="{Binding StationedCard}"/>

                                    <Border Grid.Row="1" Background="OrangeRed" BorderThickness="1" BorderBrush="Black" Width="30" Height="30" CornerRadius="50" HorizontalAlignment="Center" Margin="0,10,0,0" AllowDrop="False">
                                        <Label Content="{Binding ID}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <Grid Canvas.Left="0" Canvas.Top="840" Width="300" Height="90" Background="LightGray" Margin="10,0,10,0">
                <Grid.Resources>
                    <Style TargetType="Border">
                        <Setter Property="Height" Value="{x:Static local:Constants.CostSectorBubbleWidthHeight}"/>
                        <Setter Property="Width" Value="{x:Static local:Constants.CostSectorBubbleWidthHeight}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="CornerRadius" Value="45"/>
                        <Setter Property="BorderBrush" Value="Black"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Background="Yellow"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding HumanPlayer.Credits, Mode=OneWay}"/>

                <Border Grid.Row="1" Grid.Column="0" Background="LightGreen"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HumanPlayer.Income, Mode=OneWay}"/>

                <Border Grid.Row="2" Grid.Column="0" Background="DodgerBlue"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding HumanPlayer.VictoryPoints, Mode=OneWay}"/>
            </Grid>
        </Canvas>

        <local:DiceRollControl Grid.Row="0" Grid.RowSpan="2" Panel.ZIndex="1" Visibility="{Binding ShowDiceRollControl, Converter={StaticResource BoolVisibilityConverter}}"/>

        <Grid Grid.Row="1" Panel.ZIndex="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="LightGray" Margin="10,20,10,20">
                <Grid.Resources>
                    <Style TargetType="Border">
                        <Setter Property="Height" Value="{x:Static local:Constants.CostSectorBubbleWidthHeight}"/>
                        <Setter Property="Width" Value="{x:Static local:Constants.CostSectorBubbleWidthHeight}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="CornerRadius" Value="45"/>
                        <Setter Property="BorderBrush" Value="Black"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Background="Yellow"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding HumanPlayer.Credits, Mode=OneWay}"/>

                <Border Grid.Row="1" Grid.Column="0" Background="LightGreen"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding HumanPlayer.Income, Mode=OneWay}"/>

                <Border Grid.Row="2" Grid.Column="0" Background="DodgerBlue"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding HumanPlayer.VictoryPoints, Mode=OneWay}"/>
            </Grid>

            <Grid Grid.Column="1">
                <Button Content="Roll dice" Command="{Binding RollDiceCommand}"
                        Height="50" Width="100" HorizontalAlignment="Center"
                        Visibility="{Binding ShowDiceRollControl, Converter={StaticResource BoolVisibilityConverter}, ConverterParameter=Invert}"
                        IsEnabled="{Binding IsMainWindowActive}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
